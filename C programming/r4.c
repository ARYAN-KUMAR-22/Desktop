#include <stdio.h>
#include <string.h>

int main()
{
    char str[100];

    printf("ENTER THE STRING : ");
    gets(str);

    puts(str);
    int strl = strlen(str);
    printf("%s", nstr);

    printf("%d", strl);
    puts(nstr);

    return 0;
}